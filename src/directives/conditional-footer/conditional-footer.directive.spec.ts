import { ConditionalFooterDirective } from './conditional-footer.directive';

describe('ConditionalFooterDirective', () => {
  it('should create an instance', () => {
    const directive = new ConditionalFooterDirective();
    expect(directive).toBeTruthy();
  });
});
